#include <bits/stdc++.h>

#define optimizaIO() ios_base::sync_with_stdio(0);cin.tie(0);
#define INF INT32_MAX

using namespace std;

int main(){
    optimizaIO();
    int N, X;
    cin >> N >> X;
    vector<int> A(N);
    for(auto &e: A)cin >> e;
    
    return 0;
}