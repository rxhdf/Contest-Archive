#include <bits/stdc++.h>

using namespace std;

void solve(){
    int N;
    cin >> N;
    vector<int> A(N, 0);
    for(auto &E : A){
        cin >> E;
    }
    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int tt;
    cin >> tt;
    while(tt--){
        solve();
    }
    return 0;
}