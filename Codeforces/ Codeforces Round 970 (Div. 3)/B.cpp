#include <bits/stdc++.h>
#define ll long long int
#define number first
#define position second

using namespace std;

void solve(){
    int N;
    cin >> N;
    string S;
    cin >> S;
    int r = (int)sqrt(N);
    
    for(int i = 0; i < r; ++i){
        for(int j = i; j < i + r; ++j){

        }
    }
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int tt;
    cin >> tt;
    while(tt--){
        solve();
    }

    return 0;
}